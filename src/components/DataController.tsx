import React, { useEffect, memo, useState } from 'react';

const DataController = (props:any) => {
return (
    <div></div>
    );
}

export default (props:any) => {
    return <DataController props={props}/>;
};